node_modules/
.env*
!.env.example
.idea/
.gstack/
.context/
dist/
*.log
.next/
next-env.d.ts

.vercel
.env*

# Incremental TypeScript build cache — a machine-local artifact that churns on every
# typecheck. Tracked by accident; it is not source and its diffs are noise.
tsconfig.tsbuildinfo
*.tsbuildinfo
